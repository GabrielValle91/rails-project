<%= form_for [user, client] do |f| %>
  <% if flash[:notice] %>
    <%= flash[:notice] %>
  <% end %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :status, value="Active?"%>
  <%= f.check_box :status %>
  <br>
  <%= f.submit %>
<% end %>
